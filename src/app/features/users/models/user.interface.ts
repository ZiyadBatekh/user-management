export interface User {
  id: number;
  first_name: string;
  last_name: string;
  email: string;
  avatar: string; // Assuming avatar is a URL to the user's avatar image
  // Add other properties as needed
}
